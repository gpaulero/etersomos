import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Lectura Akáshica | Eter Somos",
  description:
    "Solicitá tu lectura personalizada del Campo Akáshico. Accedé a la sabiduría de tu alma con respuestas grabadas en audio a 2 preguntas.",
};

export default function LecturasLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
